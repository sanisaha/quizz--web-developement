import React from 'react';

const Header = () => {
    return (
        <div>
            <h4 className='text-lg'>This is header</h4>
            <button className='btn w-64 rounded-full'>Button</button>
        </div>
    );
};

export default Header;